apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: locust

#resources:
#  - configmap.yaml

helmCharts:
  - name: locust
    repo: https://charts.deliveryhero.io/
    version: 0.30.0
    releaseName: locust

configMapGenerator:
  - name: api-locustfile
    behavior: merge
    files:
      - main.py